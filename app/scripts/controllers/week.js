'use strict';

/**
 * @ngdoc function
 * @name schedulerApp.controller:WeekCtrl
 * @description
 * # WeekCtrl
 * Controller of the schedulerApp
 */
angular.module('schedulerApp')
  .controller('WeekCtrl', function () {

  	// this should accept an array from a service of scheduled appiontments and inject them into the week view, along with availabilities
  });
